'use strict';

function firstTask() {
	const arr = [3, 5, 8, 16, 20, 23, 50];
	const result = [];

	for  (let i = 0; i < arr.length; i++){
		result[i] = arr[i];
	}
	console.log(result);
	return result;
}
firstTask();

// В данной функции firstTask() происходит следующее:

// Создается массив arr, содержащий элементы [3, 5, 8, 16, 20, 23, 50].
// Создается пустой массив result, который будет содержать результат.
// Запускается цикл for, инициализируется переменная i с начальным значением 0. Цикл выполняется, пока i меньше длины массива arr.
// Внутри цикла значение элемента arr[i] копируется в соответствующий элемент result[i]. Это означает, что каждый элемент из arr копируется в тот же индекс result.
// Выводится результат result в консоль с помощью console.log().
// Функция возвращает массив result.
// Таким образом, функция firstTask() просто копирует значения из массива arr в новый массив result и выводит результат в консоль.


const data = [5, 10, 'Shopping', 20, 'Homework'];
for (let i = 0; i < data.length; i++) {
	if (typeof data[i] === 'number') {
		data[i] *= 2; // Увеличиваем числа в 2 раза
	} else if (typeof data[i] === 'string') {
		data[i] += ' - done'; // Добавляем " - done" к строкам
	}
}

console.log(data);

// Данный код выполняет следующие действия:

// Задается исходный массив data с элементами [5, 10, 'Shopping', 20, 'Homework'].
// Запускается цикл for, который итерирует по каждому элементу массива data. Инициализируется переменная i с начальным значением 0, и цикл выполняется до тех пор, пока i меньше длины массива data.
// Внутри цикла происходит проверка условий для каждого элемента data[i]:
// Если тип элемента равен 'number' (число), то значение числа умножается на 2 с помощью операции *= 2. Например, 5 станет 10, 10 станет 20, и 20 станет 40.
// Если тип элемента равен 'string' (строка), то к этой строке добавляется ' - done' с помощью операции += ' - done'. Например, 'Shopping' станет 'Shopping - done', и 'Homework' станет 'Homework - done'.
// После завершения цикла выводится измененный массив data с помощью console.log(data).
// Таким образом, код модифицирует исходный массив data, увеличивая числа в 2 раза и добавляя строке ' - done' в случае, если элемент является строкой. Результат выводится в консоль. В данном случае, после выполнения кода, массив 


const data = [5, 10, 'Shopping', 20, 'Homework'];
const result = [];

for (let i = 1; i <= data.length; i++) {
    result[i - 1] = data[data.length - i]
}

console.log(result);
return result;

// В данном коде происходит развертывание массива data наоборот с использованием цикла и запись данных в новый массив result. Давайте разберем, что происходит в этом коде:

// Создается исходный массив data с элементами [5, 10, 'Shopping', 20, 'Homework'].
// Создается пустой массив result, который будет содержать результат развертывания.
// Запускается цикл for, инициализируется переменная i с начальным значением 1. Цикл выполняется, пока i меньше или равно длине массива data.
// Внутри цикла происходит присваивание элемента data[data.length - i] в соответствующий элемент result[i - 1]. Обратите внимание, что result[i - 1] используется для записи элемента в result, чтобы сохранить порядок элементов.
// После завершения цикла выводится результат result в консоль с помощью console.log().
// Функция возвращает массив result.
// Таким образом, код разворачивает исходный массив data наоборот, сохраняя порядок элементов, и записывает его в новый массив result. После выполнения кода, массив result будет содержать элементы ['Homework', 20, 'Shopping', 10, 5], что является развернутой версией массива data.
